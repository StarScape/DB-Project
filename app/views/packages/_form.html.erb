<%= form_with(model: package, local: true) do |form| %>
  <% if package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(package.errors.count, "error") %> prohibited this package from being saved:</h2>

      <ul>
      <% package.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :received %>
    <%= form.check_box :received, id: :package_received %>
  </div>

  <div class="field">
    <%= form.label :perishable %>
    <%= form.check_box :perishable, id: :package_perishable %>
  </div>

  <div class="field">
    <%= form.label :first_email %>
    <%= form.datetime_select :first_email, id: :package_first_email %>
  </div>

  <div class="field">
    <%= form.label :second_email %>
    <%= form.datetime_select :second_email, id: :package_second_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
